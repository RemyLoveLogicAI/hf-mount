interface:
  display_name: "Hf Mount"
  short_description: "Repo-specific patterns and workflows for hf-mount"
  default_prompt: "Use the hf-mount repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true