<!-- METADATA: {"source_path": "deploy/vps-setup.sh", "source_sha": "", "extraction_quality": "raw_source", "model": "gpt-5-mini", "generated_at": "2026-08-10T09:34:44Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "deploy/vps-setup.sh",
  "source_hash": "b92c98725371b3d0ae270eef927ae15150b889121114e74d1580a386a7088bdc",
  "last_updated": "2026-08-10T09:34:44.053136+00:00",
  "tokens_used": 3065,
  "complexity_score": 6,
  "estimated_review_time_minutes": 51,
  "external_dependencies": []
}
```

</details>

[Documentation Home](../README.md) > [deploy](./README.md) > **vps-setup.sh**

---

# vps-setup.sh

> **File:** `deploy/vps-setup.sh`

![Complexity: Medium](https://img.shields.io/badge/Complexity-Medium-yellow) ![Review Time: 51min](https://img.shields.io/badge/Review_Time-51min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This is a Bash VPS deployment script (deploy/vps-setup.sh) for installing and configuring the hf-mount tooling on a server. It reads configuration from environment variables (with sensible defaults), installs prerequisites, detects an appropriate backend (FUSE or NFS), installs binaries, creates a system user and directories, writes systemd service units to automatically mount model repositories, and provides helper functionality for mounting additional repos. The script includes utility routines to build mount option strings, wait for mounts to become ready, and validate that mounts are working.

The script is defensive and operationally-oriented: it sets strict shell options (set -euo pipefail), defines a cleanup/rollback routine that undoes changes on error (stopping/disabling services, removing created directories/users/binaries), and logs progress. Many operational details are configurable via environment variables (paths, cache sizes, token locations, default repos, and backend selection). Note: the provided extraction was truncated and structural details beyond the shown functions and variables were not available, so the description is limited to the visible portions of the file.

## Dependencies

No dependencies identified.

## 📁 Directory

This file is part of the **deploy** directory. View the [directory index](_docs/deploy/README.md) to see all files in this module.

## Architecture Notes

- Uses strict shell safety flags (set -euo pipefail) and a trap to run a cleanup/rollback function on errors.
- Configuration-by-environment-variables with defaults set at the top of the script for easy customization.
- Modular design with small focused functions (check_root, check_prerequisites, detect_backend, build_mount_options, wait_for_mount, validate_mount).
- Implements platform/package-manager detection (apt-get, yum, dnf) and systemd interaction (systemctl) for service management.
- Extraction was truncated; description is based only on the visible parts of the file and may omit additional logic present later in the script.
- Documentation generated from raw source — no structural extractor was available for this language; descriptions may be less reliable.

## Maintenance Notes

- Read and apply configuration from environment variables and provide defaults for install locations, directories, cache, tokens, and backend selection.
- Check for root privileges and required prerequisites (curl, jq) and install missing packages via apt/yum/dnf when necessary.
- Detect the available backend (FUSE or NFS) and honor an explicit BACKEND override, failing if the requested backend is unavailable.
- Create/prepare system resources (user, directories, systemd service units) and install hf-mount binaries, with rollback on error.
- Provide utility functions to build mount option strings, wait for mount activation, and validate mount points.

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/deploy/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
